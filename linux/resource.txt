# 
for dir in /*; do
  du -sh $dir 2>/dev/null
done
for dir in */;do
  du -sh $dir
done
for dir in .*/;do
  du -sh $dir
done
# ram 
free -h

# disk
# df is filesystem level
df -h  # the overall disk usage in the system
df -h ~/folder # the size of partition that the folder belong to it
# du is actual file size
du -sh ~/folder # it show the space used by the foler, by default work in the current directory 
du -h # it show the space of each file even under subdirectory


# inode
df -i


# disp partition
lsblk
cfdisk
fdisk
fdisk -l
fdisk /dev/nvme0n1
p  # list partitions
d  # delete partitions
n  # create partitions
w  # write the changes
# take attention to the start and the end for each partion
### Partition #3 contains a btrfs signature. Do you want to remove the signature? [Y]es/[N]o:   # n to keep old data
btrfs filesystem resize max /
partprobe /dev/nvme0n1


# btrfs
btrfs filesystem show /
btrfs filesystem usage /
btrfs filesystem resize max /

# clean
# try ncu
ncdu / --exclude /media --exclude /run/timechift
yay -Scc
yarn clean cache
npm cache clean --force
journalctl --vacuum-time=1s
journalctl --vacuum-time=1d
journalctl --vacuum-size=50M
